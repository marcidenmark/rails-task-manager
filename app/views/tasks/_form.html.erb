<%= form_for @task do |f| %>
  <%= f.label :name, 'Name' %>
  <%= f.text_field :name, class: 'text' %>
  <%= f.check_box :done %>

  <%= f.label :done, 'Check if done' %>
  <%= f.submit 'submit' %>
<% end %>
